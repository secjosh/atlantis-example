version: 3
projects:
- dir: environment/dev
  name: environment/dev
  workflow: custom
workflows:
  custom:
    plan:
      steps:
      - run: |
            echo "====RUNNING PLAN" >> /plan-output.txt
            echo "DIR=$DIR" >>  /plan-output.txt
            echo "REL_REPO_DIR=$REL_REPO_DIR" >> /plan-output.txt
            echo "ROOT_DIR=${DIR%"$REL_REPO_DIR"}" >> /plan-output.txt
            echo "=====" >> /plan-output.txt
            echo "$(env)" >> /plan-output.txt
            /test-script.sh